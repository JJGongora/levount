<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link
    href="https://fonts.googleapis.com/css2?family=Bodoni+Moda:ital,opsz,wght@0,6..96,400..900;1,6..96,400..900&family=Cormorant+Garamond:ital,wght@0,300..700;1,300..700&family=Lato:ital,wght@0,100;0,300;0,400;0,700;0,900;1,100;1,300;1,400;1,700;1,900&family=Montserrat:ital,wght@0,100..900;1,100..900&family=Noto+Serif:ital,wght@0,100..900;1,100..900&family=Nunito+Sans:ital,opsz,wght@0,6..12,200..1000;1,6..12,200..1000&family=Playfair+Display:ital,wght@0,400..900;1,400..900&display=swap"
    rel="stylesheet">

<link rel="icon" type="image/x-icon" href="/LeVount/images/logos/LeVount_Icon_Gray.svg">
<link rel="stylesheet" href="/LeVount/styles/global.css">
<script src="/LeVount/js/global.js" defer></script>
<script src="/js/libraries/lenis/lenis.min.js"></script>

<style>
    body {
        margin: 0;
        padding: 0;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Helvetica Neue', Arial, sans-serif;
        position: relative;

        & main {}
    }
</style>

<% if(!userSession) { %>
    <script>
        function hideMessage(element) {
            const messageBox = document.getElementById('errorMessage');
            messageBox.classList.remove("Open");
        }

        async function logIn(event) {
            event.preventDefault();

            const form = document.getElementById("logIn");
            const messageBox = document.getElementById('errorMessage');

            try {

                const user = (form[0].value == "") ? null : form[0].value;
                const password = (form[1].value == "") ? null : form[1].value;
                const currentUrl = window.location.pathname + window.location.search;

                if (!user) {

                    messageBox.children[1].innerHTML = `Please, enter a username or email.`;
                    messageBox.classList.add("Open");
                    setTimeout(hideMessage, 5000);

                } else if (!password) {

                    messageBox.children[1].innerHTML = `Please, enter your password.`;
                    messageBox.classList.add("Open");
                    setTimeout(hideMessage, 5000);

                } else {
                    const res = await fetch("/api/auth/login", {
                        method: "POST",
                        headers: { "Content-Type": "application/json" },
                        body: JSON.stringify({ user, password, currentUrl })
                    });

                    const resJson = await res.json();

                    if (resJson.success) {
                        window.location.href = resJson.redirect;
                    } else {
                        messageBox.children[1].innerHTML = resJson.message;
                        messageBox.classList.add("Open");
                        setTimeout(hideMessage, 5000);
                    }
                }
            } catch (error) {
                messageBox.children[1].innerHTML = `Error: ` + error;
                messageBox.classList.add("Open");
                setTimeout(hideMessage, 5000);
            }
        }
    </script>
    <% } %>

